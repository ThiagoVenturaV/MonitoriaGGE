{
  "name": "failover-cloudflare-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker de Health Check e Failover automático Hostinger -> DigitalOcean",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
